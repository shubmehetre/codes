#include <bits/stdc++.h>
#include <vector>
using namespace std;

enum Cars {Bugatti, Audi, Bently, Rolls_Royce};

int main(){

  Cars car = Bently;

  // cout << typeid(Cars).name();

  char x = 'x';

  switch(x){

    case 'x': cout << "this is x"; break;

  }
  cout << endl;

  /* switch (car) {
    case Audi: cout << "this is a Audi"; break;
    case Bugatti: cout << "this is a Bugatti"; break;
    case Bently: cout << "this is a Bently"; break;
    case Rolls_Royce: cout << "this is a Rolls_Royce"; break;
  } */

}
